# 프런트엔드 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** FastAPI 백엔드가 정적 파일로 서빙하는 순수 HTML+JS SPA — 로그인·홈·내 출석 입력·현황판 4화면.

**Architecture:** `app/static/` 아래 파일 4개(`index.html`, `style.css`, `api.js`, `app.js`). `app.js`가 해시 라우터와 뷰 4개를 담고, `api.js`가 fetch 래퍼·세션·라벨 상수를 담는다. 백엔드 변경은 `main.py`에 `StaticFiles` 마운트 한 줄. 현황판은 5초 폴링.

**Tech Stack:** HTML, CSS, JS(ES 모듈, 빌드 없음, 의존성 0), FastAPI `StaticFiles`, pytest. Google Fonts에서 `Noto Serif KR` 1종(제목용, 없어도 동작).

**Spec:** `docs/superpowers/specs/2026-08-23-frontend-design.md`

## Global Constraints

- 프런트 의존성 0, 빌드 도구 없음. JS 테스트 프레임워크·린터 추가 금지.
- 백엔드 로직 변경 없음. `main.py`에는 정적 마운트만 추가.
- 팔레트: 네이비 `#1F2A44`, 골드 `#C9A227`, 아이보리 `#F7F4EC`. 상태색 고정: 출석 `#2E7D4F`, 지각 `#B7791F`, 결석 `#B3261E`, 미확정 `#6B7280`.
- 모바일 우선(360px~), 본문 16px, 터치 타겟 44px 이상, 가로 스크롤 없음.
- 서버 데이터(이름·사유·제목·장소·경고)는 템플릿에 넣기 전 반드시 `esc()`.
- 라벨: 파트 `soprano/alto/tenor/bass → 소프라노/알토/테너/베이스`, 역할 `member/part_leader/conductor → 단원/파트장/지휘자`, 상태 `present/late/absent/null → 출석/지각/결석/미확정`.
- pytest는 항상 `pytest --basetemp=.pytest_tmp`.
- 커밋 메시지 한국어, 끝에 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- 저장소 경로에 한글·공백이 있어 Git Bash에서 한글 경로가 깨질 수 있다. 셸 명령은 저장소 루트를 cwd로 두고 상대 경로만 쓴다. 파일 내용 작성은 Write/Edit 도구 사용(heredoc 금지).

## 개발 서버 기동 (Task 2 이후 브라우저 확인에 공통)

노션 없이 순수 SQLite로 기동한다. 기본 DB 파일은 `attendance.db`(gitignore 대상).

```bash
python -c "from tests.conftest import SEED; from app import db; c=db.connect('attendance.db'); c.executemany('INSERT OR IGNORE INTO members (name, student_id, part, role) VALUES (?,?,?,?)', SEED); c.commit(); print('seeded')"
```

```bash
python -m app.main
```

접속: `http://localhost:8000/`. 시드 계정(이름/학번): 지휘자/c1, 파트장소프라노/ps, 파트장알토/pa, 파트장테너/pt, 파트장베이스/pb, 김소/m1(소프라노 단원), 김알/m2(알토 단원).
브라우저 패널을 쓸 수 있으면 `.claude/launch.json`(Task 1에서 생성)로 `preview_start name="attendance"` 후 `resize_window preset="mobile"`.

---

### Task 1: 정적 서빙 + 셸 + 디자인 토큰

**Files:**
- Create: `app/static/index.html`
- Create: `app/static/style.css`
- Create: `tests/test_static.py`
- Create: `.claude/launch.json`
- Modify: `app/main.py:95-96` (`register_routes(app)` 직후에 마운트)
- Modify: `.gitignore`

**Interfaces:**
- Produces: `GET /` → `index.html`. `index.html`은 `<header class="top">`(`#user`, `#logout`), `<main id="view">`, `<div id="toast">`를 가지며 `/app.js`를 모듈로 로드한다. `style.css`의 클래스(`.card .stack .row .between .eyebrow .display .muted .err .big .btn .primary .small .icon .list .badge .done .chip .present .late .absent .none .warn`)를 이후 태스크가 사용한다.

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_static.py`:

```python
from tests.conftest import login


def test_index_served_at_root(client):
    r = client.get("/")
    assert r.status_code == 200
    assert r.headers["content-type"].startswith("text/html")
    assert '<main id="view">' in r.text


def test_static_assets_served(client):
    assert client.get("/app.js").status_code == 200
    assert client.get("/style.css").status_code == 200


def test_api_not_shadowed_by_static_mount(client):
    login(client, "김소", "m1")
    assert client.get("/practices").status_code == 200
    assert client.post("/auth/login",
                       json={"name": "없음", "student_id": "x"}).status_code == 401
```

- [ ] **Step 2: 실패 확인**

Run: `pytest --basetemp=.pytest_tmp tests/test_static.py -v`
Expected: 앞의 두 테스트 FAIL (404), 세 번째는 PASS.

- [ ] **Step 3: `.gitignore`에 SQLite 부속 파일 추가**

`.gitignore` 끝에 두 줄 추가:

```
*.db-shm
*.db-wal
```

- [ ] **Step 4: `app/static/index.html` 작성**

```html
<!doctype html>
<html lang="ko">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>글리 출석</title>
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Noto+Serif+KR:wght@600&display=swap" rel="stylesheet">
<link rel="stylesheet" href="/style.css">
</head>
<body>
<header class="top">
  <a href="#/home" class="brand">글리 출석</a>
  <span id="user"></span>
  <button type="button" id="logout" class="link" hidden>로그아웃</button>
</header>
<main id="view"></main>
<div id="toast" role="status" aria-live="polite"></div>
<script type="module" src="/app.js"></script>
</body>
</html>
```

- [ ] **Step 5: `app/static/style.css` 작성 (기본 토큰·컴포넌트)**

```css
:root {
  --navy: #1F2A44; --gold: #C9A227; --ivory: #F7F4EC; --card: #fff;
  --ink: #1C1C1C; --muted: #6B7280; --line: #d6d3cc;
  --present: #2E7D4F; --late: #B7791F; --absent: #B3261E; --none: #6B7280;
  --font: -apple-system, "Apple SD Gothic Neo", "Malgun Gothic", system-ui, sans-serif;
  --display: "Noto Serif KR", "Apple SD Gothic Neo", serif;
}
* { box-sizing: border-box; }
body { margin: 0; background: var(--ivory); color: var(--ink); font: 16px/1.5 var(--font); }

.top { background: var(--navy); color: #fff; display: flex; align-items: center; gap: 12px; padding: 12px 16px; }
.brand { font-family: var(--display); font-weight: 600; font-size: 20px; color: var(--gold); text-decoration: none; margin-right: auto; }
#user { font-size: 14px; opacity: .85; }
.link { background: none; border: 0; color: #fff; font: inherit; font-size: 14px; text-decoration: underline; padding: 8px; cursor: pointer; }

main { max-width: 640px; margin: 0 auto; padding: 16px; display: grid; gap: 16px; }
.card { background: var(--card); border-radius: 12px; padding: 16px; }
.stack { display: grid; gap: 12px; }
.row { display: flex; gap: 8px; align-items: center; flex-wrap: wrap; }
.between { justify-content: space-between; }
.eyebrow { margin: 0; font-size: 12px; letter-spacing: .08em; color: var(--gold); font-weight: 600; }
.display { font-family: var(--display); font-weight: 600; margin: 4px 0; }
h1.display { font-size: 26px; }
h2 { font-size: 18px; margin: 0; }
h2.display { font-size: 22px; }
.muted { color: var(--muted); margin: 0; font-size: 14px; }
.err { color: var(--absent); margin: 0; font-size: 14px; }
.big { font-size: 48px; font-family: var(--display); margin: 0; line-height: 1.1; }
.big span { font-size: 20px; margin-left: 2px; }

label { display: flex; flex-direction: column; gap: 4px; font-size: 14px; color: var(--muted); }
input { font: inherit; padding: 10px 12px; border: 1px solid var(--line); border-radius: 8px; background: #fff; color: var(--ink); min-height: 44px; width: 100%; }
input:focus-visible, button:focus-visible, a:focus-visible { outline: 3px solid var(--gold); outline-offset: 2px; }

.btn { display: inline-flex; align-items: center; justify-content: center; min-height: 44px; padding: 0 18px; border-radius: 8px; border: 1px solid var(--navy); background: #fff; color: var(--navy); font: inherit; font-weight: 600; text-decoration: none; cursor: pointer; }
.btn.primary { background: var(--navy); color: #fff; }
.btn.small { min-height: 36px; padding: 0 12px; font-size: 14px; }
.btn:disabled { opacity: .45; cursor: not-allowed; }
.icon { background: none; border: 0; font-size: 18px; min-width: 44px; min-height: 44px; cursor: pointer; color: var(--navy); }

.list { list-style: none; margin: 0; padding: 0; display: grid; gap: 8px; }
.list > li { background: var(--card); border-radius: 12px; display: flex; align-items: center; gap: 8px; padding-right: 8px; }
.list > li > a { flex: 1; display: grid; padding: 12px 16px; color: inherit; text-decoration: none; min-height: 44px; }
.list > li.closed { opacity: .6; }
.badge { font-size: 12px; padding: 4px 8px; border-radius: 999px; background: var(--ivory); color: var(--muted); white-space: nowrap; }
.badge.done { background: var(--present); color: #fff; }
.chips { display: flex; gap: 6px; flex-wrap: wrap; margin-top: 8px; }
.chip { font-size: 13px; font-weight: 600; padding: 3px 10px; border-radius: 999px; color: #fff; background: var(--none); white-space: nowrap; }
.chip.present { background: var(--present); }
.chip.late { background: var(--late); }
.chip.absent { background: var(--absent); }
.warn { background: #FFF4D6; border: 1px solid var(--late); border-radius: 12px; padding: 12px; font-size: 14px; }

dialog { border: 0; border-radius: 16px 16px 0 0; width: 100%; max-width: 640px; margin: auto auto 0; padding: 20px; }
dialog::backdrop { background: rgba(31, 42, 68, .5); }

#toast { position: fixed; right: 16px; bottom: 16px; max-width: calc(100% - 32px); background: var(--navy); color: #fff; padding: 12px 16px; border-radius: 8px; opacity: 0; transition: opacity .2s; pointer-events: none; }
#toast.show { opacity: 1; }
#toast.error { border: 2px solid var(--absent); }
@media (prefers-reduced-motion: reduce) { #toast { transition: none; } }
```

- [ ] **Step 6: 빈 `app/static/app.js` 생성 (Task 2에서 채움)**

내용 한 줄:

```js
// Task 2에서 작성
```

- [ ] **Step 7: `app/main.py`에 마운트 추가**

import 블록에 추가:

```python
from pathlib import Path

from fastapi.staticfiles import StaticFiles
```

`create_app` 안 `register_routes(app)` 바로 아래(`return app` 앞):

```python
    register_routes(app)
    app.mount("/", StaticFiles(directory=Path(__file__).parent / "static", html=True),
              name="static")
    return app
```

- [ ] **Step 8: 테스트 통과 확인 + 전체 회귀**

Run: `pytest --basetemp=.pytest_tmp -q`
Expected: 전부 PASS (기존 테스트 포함). 마운트가 API를 가리면 기존 테스트가 깨진다.

- [ ] **Step 9: `.claude/launch.json` 생성**

```json
{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "attendance",
      "runtimeExecutable": "python",
      "runtimeArgs": ["-m", "app.main"],
      "port": 8000
    }
  ]
}
```

- [ ] **Step 10: 커밋**

```bash
git add .gitignore .claude/launch.json app/main.py app/static tests/test_static.py
git commit -m "feat: 정적 서빙 마운트 + 프런트 셸·디자인 토큰"
```

---

### Task 2: api.js + 라우터·헤더·토스트 + 로그인 뷰

**Files:**
- Create: `app/static/api.js`
- Modify: `app/static/app.js` (전체 교체)

**Interfaces:**
- Produces (`api.js`): `api(method, path, body?) → Promise<json>` (401이면 세션 삭제 + `#/login` 이동 후 throw; 그 외 `!ok`면 `ApiError{status, detail}` throw), `class ApiError extends Error {status, detail}`, `session()/setSession(obj)/clearSession()`, `esc(str)`, `fmtDate(iso) → "M/D (요일) HH:MM"`, 상수 `PART, PARTS, ROLE, STATUS`.
- Produces (`app.js`): `view` (= `#view`), `route()`, `toast(msg, error=false)`, 뷰 테이블 `routes`에 Task 3~5가 `homeView/practiceView/boardView`를 추가. 뷰 함수 시그니처: `async function xxxView(id?: number) → (cleanup: () => void) | undefined`.

- [ ] **Step 1: `app/static/api.js` 작성**

```js
export const PARTS = ['soprano', 'alto', 'tenor', 'bass'];
export const PART = { soprano: '소프라노', alto: '알토', tenor: '테너', bass: '베이스' };
export const ROLE = { member: '단원', part_leader: '파트장', conductor: '지휘자' };
export const STATUS = { present: '출석', late: '지각', absent: '결석', null: '미확정' };

export function session() {
  try { return JSON.parse(localStorage.getItem('session')); } catch { return null; }
}
export function setSession(s) { localStorage.setItem('session', JSON.stringify(s)); }
export function clearSession() { localStorage.removeItem('session'); }

export class ApiError extends Error {
  constructor(status, detail) {
    super(typeof detail === 'string' ? detail : JSON.stringify(detail));
    this.status = status;
    this.detail = detail;
  }
}

export async function api(method, path, body) {
  const res = await fetch(path, {
    method,
    headers: body ? { 'Content-Type': 'application/json' } : {},
    body: body ? JSON.stringify(body) : undefined,
    credentials: 'same-origin',
  });
  if (res.status === 401) {
    clearSession();
    location.hash = '#/login';
    throw new ApiError(401, '로그인 필요');
  }
  const data = await res.json().catch(() => ({}));
  if (!res.ok) throw new ApiError(res.status, data.detail ?? res.statusText);
  return data;
}

const ESC = { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' };
export function esc(s) { return String(s ?? '').replace(/[&<>"']/g, c => ESC[c]); }

const DAYS = '일월화수목금토';
export function fmtDate(iso) {
  const d = new Date(iso);
  const hh = String(d.getHours()).padStart(2, '0');
  const mm = String(d.getMinutes()).padStart(2, '0');
  return `${d.getMonth() + 1}/${d.getDate()} (${DAYS[d.getDay()]}) ${hh}:${mm}`;
}
```

- [ ] **Step 2: `app/static/app.js` 작성 (라우터·헤더·토스트·로그인)**

```js
import { api, session, setSession, clearSession, esc, fmtDate, PART, PARTS, ROLE, STATUS, ApiError } from './api.js';

const view = document.getElementById('view');
let cleanup = null;
let toastTimer;

export function toast(msg, error = false) {
  const el = document.getElementById('toast');
  el.textContent = msg;
  el.className = error ? 'show error' : 'show';
  clearTimeout(toastTimer);
  toastTimer = setTimeout(() => { el.className = ''; }, 3000);
}

function renderHeader() {
  const s = session();
  document.getElementById('user').textContent = s ? `${s.name} · ${PART[s.part]} · ${ROLE[s.role]}` : '';
  document.getElementById('logout').hidden = !s;
}
document.getElementById('logout').onclick = () => { clearSession(); location.hash = '#/login'; };

// ---------- 로그인 ----------
async function loginView() {
  if (session()) { location.hash = '#/home'; return; }
  view.innerHTML = `
    <form id="login" class="card stack">
      <p class="eyebrow">이름과 학번으로 로그인</p>
      <label>이름 <input name="name" required autocomplete="name"></label>
      <label>학번 <input name="student_id" required autocomplete="off"></label>
      <p id="login-err" class="err" hidden>명단에 없어요. 이름과 학번을 확인하세요.</p>
      <button class="btn primary">로그인</button>
    </form>`;
  view.querySelector('#login').onsubmit = async e => {
    e.preventDefault();
    const f = e.target.elements;
    try {
      setSession(await api('POST', '/auth/login', { name: f.name.value, student_id: f.student_id.value }));
      location.hash = '#/home';
    } catch (err) {
      if (err.status === 401) view.querySelector('#login-err').hidden = false;
      else toast(err.message, true);
    }
  };
}

// ---------- 라우터 ----------
const routes = [
  [/^#\/login$/, loginView],
];

async function route() {
  if (cleanup) { cleanup(); cleanup = null; }
  const hash = location.hash || '#/home';
  const match = routes.find(([re]) => re.test(hash));
  if (!match) { location.hash = '#/home'; return; }
  const [re, fn] = match;
  if (fn !== loginView && !session()) { location.hash = '#/login'; return; }
  renderHeader();
  view.innerHTML = '<p class="muted">불러오는 중…</p>';
  try {
    cleanup = (await fn(...hash.match(re).slice(1).map(Number))) || null;
  } catch (e) {
    if (e.status === 404) { toast('연습이 없어요', true); location.hash = '#/home'; }
    else if (e.status !== 401) toast(e.message, true);
  }
}
window.addEventListener('hashchange', route);
route();
```

주의: `routes`에 `#/home`이 아직 없으므로 이 시점에는 로그인 후 `#/home`으로 가면 다시 `#/home`으로 리다이렉트 루프가 생긴다. Step 3에서 임시 홈 뷰를 넣어 막는다.

- [ ] **Step 3: 임시 홈 뷰 추가 (Task 3에서 교체)**

`routes` 배열을 다음으로 교체:

```js
async function homeView() {
  const s = session();
  view.innerHTML = `<section class="card"><h1 class="display">${esc(s.name)}</h1><p class="muted">홈은 Task 3에서 만든다</p></section>`;
}

const routes = [
  [/^#\/login$/, loginView],
  [/^#\/home$/, homeView],
];
```

- [ ] **Step 4: 문법 검사**

Run: `node --check app/static/app.js && node --check app/static/api.js && echo OK`
Expected: `OK`

- [ ] **Step 5: 브라우저 확인**

개발 서버 기동(문서 상단 절차) 후:
1. `http://localhost:8000/` → `#/login`으로 이동, 로그인 폼 표시, 헤더에 사용자·로그아웃 없음.
2. 이름 `아무개`, 학번 `zz` → 빨간 인라인 "명단에 없어요…" 표시.
3. 이름 `김소`, 학번 `m1` → `#/home`, 헤더에 `김소 · 소프라노 · 단원`과 로그아웃 버튼.
4. 새로고침 → 여전히 홈(localStorage 세션 유지).
5. 로그아웃 → `#/login`, 헤더 사용자 정보 사라짐.
6. 주소창에 `#/nope` 입력 → `#/home`으로 교정.

- [ ] **Step 6: 커밋**

```bash
git add app/static/api.js app/static/app.js
git commit -m "feat: api 래퍼·해시 라우터·토스트·로그인 화면"
```

---

### Task 3: 홈 뷰 (다음 연습·출석률·목록·지휘자 연습 CRUD)

**Files:**
- Modify: `app/static/app.js` (Task 2의 임시 `homeView` 교체)

**Interfaces:**
- Consumes: `api, session, esc, fmtDate, toast, route, view` (Task 2).
- Produces: `nextPractice(list)`, `practiceLink(p, role)` — 이 태스크 내부에서만 사용.

- [ ] **Step 1: 임시 `homeView`를 아래 코드로 교체**

```js
// ---------- 홈 ----------
function practiceLink(p, role) {
  return role === 'member' ? `#/practice/${p.id}` : `#/board/${p.id}`;
}

function nextPractice(list) {
  const today = new Date(); today.setHours(0, 0, 0, 0);
  const open = list.filter(p => p.status === 'open');
  const upcoming = open.filter(p => new Date(p.starts_at) >= today)
    .sort((a, b) => a.starts_at.localeCompare(b.starts_at));
  return upcoming[0] || open[0] || null; // 목록은 최신순이라 open[0]이 가장 최근
}

function when(p) { return `${fmtDate(p.starts_at)}${p.place ? ' · ' + esc(p.place) : ''}`; }

async function homeView() {
  const s = session();
  const [list, stats] = await Promise.all([api('GET', '/practices'), api('GET', '/me/stats')]);
  const next = nextPractice(list);
  const staff = s.role !== 'member';
  const conductor = s.role === 'conductor';
  view.innerHTML = `
    ${next ? `
    <section class="card">
      <p class="eyebrow">다음 연습</p>
      <h2 class="display">${esc(next.title)}</h2>
      <p>${when(next)}</p>
      <div class="row">
        <a class="btn primary" href="#/practice/${next.id}">내 출석 입력</a>
        ${staff ? `<a class="btn" href="#/board/${next.id}">현황판</a>` : ''}
      </div>
    </section>` : ''}
    <section class="card">
      <p class="eyebrow">내 출석률</p>
      ${stats.total
        ? `<p class="big">${Math.round(stats.rate * 100)}<span>%</span></p>
           <p class="muted">출석 ${stats.present} · 지각 ${stats.late} · 결석 ${stats.absent} / 총 ${stats.total}</p>`
        : '<p class="muted">아직 마감된 연습이 없어요</p>'}
    </section>
    <section class="stack">
      <div class="row between">
        <h2>연습</h2>
        ${conductor ? '<button type="button" id="new" class="btn small">새 연습</button>' : ''}
      </div>
      ${list.length ? `<ul class="list">${list.map(p => `
        <li class="${p.status}">
          <a href="${practiceLink(p, s.role)}">
            <strong>${esc(p.title)}</strong>
            <span class="muted">${when(p)}</span>
          </a>
          <span class="badge">${p.status === 'open' ? '진행중' : '마감'}</span>
          ${conductor && p.status === 'open' ? `
            <button type="button" class="icon" data-edit="${p.id}" aria-label="수정">✎</button>
            <button type="button" class="icon" data-del="${p.id}" aria-label="삭제">✕</button>` : ''}
        </li>`).join('')}</ul>` : '<p class="muted">연습이 없어요</p>'}
    </section>
    <dialog id="pform">
      <form class="stack">
        <h2 id="pform-title">새 연습</h2>
        <label>제목 <input name="title" required></label>
        <label>시작 <input name="starts_at" type="datetime-local" required></label>
        <label>장소 <input name="place"></label>
        <div class="row">
          <button type="button" id="pform-cancel" class="btn">취소</button>
          <button class="btn primary">저장</button>
        </div>
      </form>
    </dialog>`;

  if (!conductor) return;
  const dlg = view.querySelector('#pform');
  const form = dlg.querySelector('form');
  const f = form.elements; // form.title은 HTML 속성이라 elements로 접근
  const openForm = p => {
    form.reset();
    form.dataset.id = p ? p.id : '';
    dlg.querySelector('#pform-title').textContent = p ? '연습 수정' : '새 연습';
    if (p) { f.title.value = p.title; f.starts_at.value = p.starts_at.slice(0, 16); f.place.value = p.place; }
    dlg.showModal();
  };
  view.querySelector('#new').onclick = () => openForm(null);
  view.querySelector('#pform-cancel').onclick = () => dlg.close();
  view.querySelectorAll('[data-edit]').forEach(b => {
    b.onclick = () => openForm(list.find(p => p.id === Number(b.dataset.edit)));
  });
  view.querySelectorAll('[data-del]').forEach(b => {
    b.onclick = async () => {
      if (!confirm('이 연습을 삭제할까요?')) return;
      try { await api('DELETE', `/practices/${b.dataset.del}`); toast('삭제했어요'); route(); }
      catch (e) { toast(e.message, true); }
    };
  });
  form.onsubmit = async e => {
    e.preventDefault();
    const id = form.dataset.id;
    const body = { title: f.title.value, starts_at: f.starts_at.value, place: f.place.value };
    try {
      await api(id ? 'PUT' : 'POST', id ? `/practices/${id}` : '/practices', body);
      dlg.close(); toast('저장했어요'); route();
    } catch (err) { toast(err.message, true); }
  };
}
```

- [ ] **Step 2: 문법 검사**

Run: `node --check app/static/app.js && echo OK`
Expected: `OK`

- [ ] **Step 3: 브라우저 확인**

1. `지휘자/c1` 로그인 → 홈에 "연습이 없어요", 출석률 카드 "아직 마감된 연습이 없어요", "새 연습" 버튼.
2. 새 연습 → 제목 `정기 연습`, 시작 = 내일 19:00, 장소 `음악관 301` → 저장 → 토스트 "저장했어요", 목록과 "다음 연습" 카드에 표시, 날짜 형식 `M/D (요일) 19:00`.
3. 새 연습 → 제목 공백만 입력 → 서버 422 토스트 "title 필수" (HTML required는 공백을 통과시킴).
4. ✎ → 폼에 기존 값 채워짐(시작 시각 포함) → 제목 수정 → 저장 → 목록 반영.
5. 다음 연습 카드에 "내 출석 입력"과 "현황판" 버튼 둘 다 표시(지휘자).
6. 로그아웃 → `김소/m1` 로그인 → 새 연습·✎·✕ 없음, 카드에 "내 출석 입력"만.
7. 지휘자로 다시 로그인 → ✕ → confirm → 삭제 → "연습이 없어요".
8. 모바일 뷰포트(375px)에서 가로 스크롤 없음.

- [ ] **Step 4: 커밋**

```bash
git add app/static/app.js
git commit -m "feat: 홈 화면 - 다음 연습·출석률·연습 목록·지휘자 연습 관리"
```

---

### Task 4: 상태 폼 컴포넌트 + 내 출석 입력 뷰

**Files:**
- Modify: `app/static/app.js` (`statusForm`, `practiceView` 추가, `routes`에 등록)
- Modify: `app/static/style.css` (세그먼트·상태 폼 CSS 추가)

**Interfaces:**
- Consumes: `api, session, esc, fmtDate, toast, route, view, when, STATUS, ApiError`.
- Produces: `statusForm(initial: {status, reason, eta}, locked: boolean) → HTMLFormElement` — `form.read() → {status, reason, eta}`, 저장 버튼은 locked면 없음, 지각 필수값 비면 비활성. Task 5가 수정 시트에서 재사용한다.

- [ ] **Step 1: `style.css` 끝에 추가**

```css
.segment { position: relative; display: grid; grid-template-columns: repeat(3, 1fr); border: 1px solid var(--navy); border-radius: 8px; overflow: hidden; }
.segment label { flex-direction: row; }
.segment input { position: absolute; opacity: 0; width: 0; height: 0; min-height: 0; }
.segment span { display: block; width: 100%; text-align: center; padding: 12px 0; min-height: 44px; color: var(--navy); font-weight: 600; cursor: pointer; }
.segment input:checked + span { background: var(--gold); color: var(--navy); }
.segment input:focus-visible + span { outline: 3px solid var(--gold); outline-offset: -3px; }
.segment input:disabled + span { opacity: .5; cursor: default; }
.status-form .f-reason, .status-form .f-eta { display: none; }
.status-form[data-status="late"] .f-reason, .status-form[data-status="late"] .f-eta,
.status-form[data-status="absent"] .f-reason { display: flex; }
.current { font-weight: 600; margin: 0; }
.current.present { color: var(--present); }
.current.late { color: var(--late); }
.current.absent { color: var(--absent); }
```

- [ ] **Step 2: `app.js`의 홈 블록 아래에 상태 폼 + 내 출석 입력 뷰 추가**

```js
// ---------- 상태 폼 (단원 본인 입력 · 현황판 수정 시트 공용) ----------
function statusForm(initial, locked) {
  const form = document.createElement('form');
  form.className = 'status-form stack';
  const dis = locked ? 'disabled' : '';
  form.innerHTML = `
    <div class="segment" role="radiogroup" aria-label="출석 상태">
      ${['present', 'late', 'absent'].map(v => `
        <label><input type="radio" name="status" value="${v}" ${initial.status === v ? 'checked' : ''} ${dis}><span>${STATUS[v]}</span></label>`).join('')}
    </div>
    <label class="f-reason">사유 <input name="reason" value="${esc(initial.reason)}" placeholder="예: 수업, 버스 지연" ${dis}></label>
    <label class="f-eta">도착 예정 <input name="eta" type="time" value="${esc(initial.eta)}" ${dis}></label>
    ${locked ? '' : '<button class="btn primary">저장</button>'}`;
  const f = form.elements;
  const sync = () => {
    const st = f.status.value;
    form.dataset.status = st;
    const btn = form.querySelector('button');
    if (btn) btn.disabled = !st || (st === 'late' && !(f.reason.value.trim() && f.eta.value));
  };
  form.oninput = sync;
  sync();
  form.read = () => {
    const st = f.status.value;
    return {
      status: st,
      reason: st === 'present' ? null : (f.reason.value.trim() || null),
      eta: st === 'late' ? f.eta.value : null,
    };
  };
  return form;
}

// ---------- 내 출석 입력 ----------
function describe(me) {
  if (me.status == null) return '미입력';
  let s = STATUS[me.status] + (me.source === 'auto' ? '(자동)' : '');
  if (me.status === 'late') s += ` · ${esc(me.reason)} · 도착 ${esc(me.eta)}`;
  else if (me.reason) s += ` · ${esc(me.reason)}`;
  return s;
}

async function practiceView(id) {
  const [list, me] = await Promise.all([api('GET', '/practices'), api('GET', `/practices/${id}/me`)]);
  const p = list.find(x => x.id === id);
  if (!p) throw new ApiError(404, '연습 없음');
  const started = new Date() >= new Date(p.starts_at);
  const closed = p.status === 'closed';
  view.innerHTML = `
    <section class="card">
      <p class="eyebrow">${closed ? '마감' : '진행중'}</p>
      <h1 class="display">${esc(p.title)}</h1>
      <p>${when(p)}</p>
    </section>
    <section class="card stack">
      <p class="eyebrow">내 상태</p>
      <p class="current ${me.status ?? ''}">${describe(me)}</p>
      ${closed ? '<p class="muted">마감된 연습입니다.</p>'
        : started ? '<p class="muted">연습이 시작돼 파트장·지휘자만 수정할 수 있어요.</p>' : ''}
      <div id="form"></div>
    </section>`;
  const form = statusForm(me, closed || started);
  view.querySelector('#form').replaceWith(form);
  form.onsubmit = async e => {
    e.preventDefault();
    try { await api('PUT', `/practices/${id}/me`, form.read()); toast('저장했어요'); route(); }
    catch (err) { toast(err.message, true); if (err.status === 403 || err.status === 409) route(); }
  };
}
```

`routes`에 추가:

```js
  [/^#\/practice\/(\d+)$/, practiceView],
```

- [ ] **Step 3: 문법 검사**

Run: `node --check app/static/app.js && echo OK`
Expected: `OK`

- [ ] **Step 4: 브라우저 확인**

사전: 지휘자로 내일 19:00 연습 A, 어제 19:00 연습 B(과거) 생성.
1. `김소/m1` 로그인 → 연습 A 탭 → "내 상태: 미입력", 세그먼트 3개, 저장 버튼 비활성.
2. 출석 선택 → 저장 활성 → 저장 → 토스트, "내 상태: 출석".
3. 지각 선택 → 사유·도착 예정 필드 나타남, 둘 다 채우기 전엔 저장 비활성 → `버스`, `19:20` → 저장 → "지각 · 버스 · 도착 19:20".
4. 결석 선택 → 사유만 보임(선택), 저장 활성 → 저장 → "결석 · 버스"(사유 유지됨) 또는 사유 비우면 "결석".
5. 새로고침 → 값 유지(서버 조회).
6. 연습 B(과거) → "결석(자동)", 안내문 "연습이 시작돼…", 세그먼트 비활성, 저장 버튼 없음.
7. 주소창 `#/practice/999` → 토스트 "연습이 없어요" → 홈.
8. 모바일 뷰포트에서 세그먼트 3칸이 한 줄, 가로 스크롤 없음.

- [ ] **Step 5: 커밋**

```bash
git add app/static/app.js app/static/style.css
git commit -m "feat: 상태 폼 컴포넌트 + 단원 본인 출석 입력 화면"
```

---

### Task 5: 현황판 (폴링·정렬·수정 시트·확인·마감·재오픈·동기화)

**Files:**
- Modify: `app/static/app.js` (`boardView` 추가, `routes`에 등록)
- Modify: `app/static/style.css` (파트 섹션·단원 행 CSS 추가)

**Interfaces:**
- Consumes: `api, session, esc, fmtDate, toast, view, when, statusForm, PART, PARTS, STATUS`.
- Produces: `sortMembers(members)` — 미확정 → 지각 → 결석 → 출석, 같은 그룹은 서버 순서 유지. `boardView(id)`는 cleanup(폴링 정지)을 반환.

- [ ] **Step 1: `style.css` 끝에 추가**

```css
.part { border-left: 3px solid var(--gold); padding-left: 12px; display: grid; gap: 8px; }
.part h2 { font-family: var(--display); font-weight: 600; }
.part small { display: block; font-family: var(--font); font-weight: 400; font-size: 13px; }
.m > button { all: unset; box-sizing: border-box; width: 100%; display: grid; grid-template-columns: 1fr auto; align-items: center; gap: 2px 8px; padding: 12px 16px; min-height: 44px; cursor: pointer; }
.m > button > .muted { grid-column: 1 / -1; }
.m > button:disabled { cursor: default; }
.m > button:focus-visible { outline: 3px solid var(--gold); outline-offset: -3px; border-radius: 12px; }
.m.none, .m.late { box-shadow: inset 4px 0 0 var(--gold); }
```

- [ ] **Step 2: `app.js`의 내 출석 입력 블록 아래에 현황판 추가**

```js
// ---------- 현황판 ----------
const ORDER = { null: 0, late: 1, absent: 2, present: 3 }; // 확인 필요 우선
function sortMembers(members) {
  return [...members].sort((a, b) => ORDER[a.status] - ORDER[b.status]); // 안정 정렬 → 그룹 안은 이름순 유지
}

async function boardView(id) {
  const s = session();
  if (s.role === 'member') { toast('파트장·지휘자만 볼 수 있어요', true); location.hash = '#/home'; return; }
  const conductor = s.role === 'conductor';
  view.innerHTML = `
    <div id="board-head"></div>
    <div id="board-list" class="stack"></div>
    <dialog id="edit">
      <div class="stack">
        <h2 id="edit-name"></h2>
        <div id="edit-form"></div>
        <button type="button" id="edit-cancel" class="btn">취소</button>
      </div>
    </dialog>`;
  const dlg = view.querySelector('#edit');
  view.querySelector('#edit-cancel').onclick = () => dlg.close();
  let data;

  async function load() {
    try { data = await api('GET', `/practices/${id}/board`); }
    catch (e) {
      if (e.status === 403 || e.status === 404) { toast(e.status === 404 ? '연습이 없어요' : e.message, true); location.hash = '#/home'; }
      return; // 네트워크 오류 등은 조용히 다음 주기에 재시도
    }
    render();
  }

  async function act(promise, okMsg) {
    try { await promise; if (okMsg) toast(okMsg); }
    catch (e) {
      const mp = e.detail && e.detail.missing_parts;
      toast(mp ? `${mp.map(k => PART[k]).join('·')} 확인이 필요해요` : e.message, true);
    }
    load();
  }

  function render() {
    const p = data.practice, open = p.status === 'open';
    const parts = PARTS.filter(k => data.parts[k]);
    const missing = parts.filter(k => !data.parts[k].confirmed);
    const allConfirmed = conductor && missing.length === 0;
    const now = new Date().toTimeString().slice(0, 8);
    view.querySelector('#board-head').innerHTML = `
      ${data.roster_warnings.length ? `<div class="warn">명단 확인 필요<br>${data.roster_warnings.map(esc).join('<br>')}</div>` : ''}
      <section class="card">
        <p class="eyebrow">${open ? '진행중' : '마감'} · 갱신 ${now}</p>
        <h1 class="display">${esc(p.title)}</h1>
        <p>${when(p)}</p>
        <div class="chips">
          <span class="chip present">출석 ${data.totals.present}</span>
          <span class="chip late">지각 ${data.totals.late}</span>
          <span class="chip absent">결석 ${data.totals.absent}</span>
          <span class="chip none">미확정 ${data.totals.unconfirmed}</span>
        </div>
        ${!conductor ? '' : open ? `
          <div class="row" style="margin-top:12px">
            <button type="button" id="close" class="btn primary" ${allConfirmed ? '' : 'disabled'}>마감</button>
            ${allConfirmed ? '' : `<p class="muted">${missing.map(k => PART[k]).join('·')} 확인 대기</p>`}
          </div>` : `
          <p class="muted" style="margin-top:12px">${p.notion_synced_at ? `노션 동기화 완료 ${p.notion_synced_at.slice(11, 16)}` : '노션 동기화 중…'}</p>
          <div class="row">
            ${p.notion_synced_at ? '' : '<button type="button" id="resync" class="btn">동기화 재시도</button>'}
            <button type="button" id="reopen" class="btn">재오픈</button>
          </div>`}
      </section>`;
    view.querySelector('#board-list').innerHTML = parts.map(k => {
      const part = data.parts[k], c = part.counts;
      return `
      <section class="part">
        <header class="row between">
          <h2>${PART[k]}<small class="muted">출석 ${c.present} · 지각 ${c.late} · 결석 ${c.absent} · 미확정 ${c.unconfirmed}</small></h2>
          ${part.confirmed ? '<span class="badge done">확인 완료</span>'
            : open ? `<button type="button" class="btn small" data-confirm="${k}">확인 완료</button>` : ''}
        </header>
        <ul class="list">${sortMembers(part.members).map(m => `
          <li class="m ${m.status ?? 'none'}">
            <button type="button" data-member="${m.member_id}" ${open ? '' : 'disabled'}>
              <strong>${esc(m.name)}</strong>
              <span class="chip ${m.status ?? 'none'}">${STATUS[m.status]}${m.source === 'auto' ? ' · 자동' : ''}</span>
              ${m.status === 'late' ? `<span class="muted">${esc(m.reason)} · 도착 ${esc(m.eta)}</span>`
                : m.reason ? `<span class="muted">${esc(m.reason)}</span>` : ''}
            </button>
          </li>`).join('')}</ul>
      </section>`;
    }).join('');
    bind();
  }

  function bind() {
    const q = sel => view.querySelector(sel);
    if (q('#close')) q('#close').onclick = () => {
      if (confirm('마감하면 미확정 단원은 결석 처리됩니다. 마감할까요?')) act(api('POST', `/practices/${id}/close`), '마감했어요');
    };
    if (q('#resync')) q('#resync').onclick = () => act(api('POST', `/practices/${id}/close`), '동기화를 다시 요청했어요');
    if (q('#reopen')) q('#reopen').onclick = () => {
      if (confirm('연습을 다시 열까요?')) act(api('POST', `/practices/${id}/reopen`), '다시 열었어요');
    };
    view.querySelectorAll('[data-confirm]').forEach(b => {
      b.onclick = () => {
        if (confirm(`${PART[b.dataset.confirm]} 확인 완료로 표시할까요?`)) act(api('POST', `/practices/${id}/part/confirm`, { part: b.dataset.confirm }), '확인 완료');
      };
    });
    view.querySelectorAll('[data-member]').forEach(b => {
      b.onclick = () => {
        const mid = Number(b.dataset.member);
        const m = Object.values(data.parts).flatMap(x => x.members).find(x => x.member_id === mid);
        q('#edit-name').textContent = m.name;
        const form = statusForm(m, false);
        q('#edit-form').replaceChildren(form);
        form.onsubmit = e => { e.preventDefault(); dlg.close(); act(api('PUT', `/practices/${id}/members/${mid}`, form.read()), '저장했어요'); };
        dlg.showModal();
      };
    });
  }

  const timer = setInterval(load, 5000);
  await load();
  return () => clearInterval(timer);
}
```

`routes`에 추가:

```js
  [/^#\/board\/(\d+)$/, boardView],
```

- [ ] **Step 3: 문법 검사**

Run: `node --check app/static/app.js && echo OK`
Expected: `OK`

- [ ] **Step 4: 브라우저 확인 — 파트장**

사전: 지휘자로 내일 19:00 연습 A 생성. `김소/m1`로 연습 A에 지각(`버스`, `19:20`) 입력.
1. `파트장소프라노/ps` 로그인 → 홈 카드 "현황판" → 소프라노 섹션만, 집계 칩, 골드 왼쪽 선.
2. 행 순서: 미확정(지휘자·파트장소프라노) → 지각(김소). 김소 행에 `버스 · 도착 19:20`.
3. 김소 행 탭 → 바텀 시트에 이름·지각 폼(값 채워짐) → 결석 선택 → 저장 → 시트 닫힘, 토스트, 행이 결석 칩으로.
4. 헤더 "갱신 HH:MM:SS"가 5초마다 바뀜.
5. "확인 완료" → confirm → 배지 "확인 완료"로 바뀜.
6. 주소창으로 `#/board/<A>`에 `김알/m2`(단원) 접근 → 토스트 "파트장·지휘자만 볼 수 있어요" → 홈.

- [ ] **Step 5: 브라우저 확인 — 지휘자**

1. `지휘자/c1` → 현황판: 4파트 전부, 마감 버튼 비활성 + "알토·테너·베이스 확인 대기".
2. 알토·테너·베이스 "확인 완료" 대행 → 마감 버튼 활성.
3. 마감 → confirm → 토스트 "마감했어요" → 배지 "마감", 미확정이던 단원들이 "결석 · 자동", 단원 행 탭 비활성, "노션 동기화 중…" + "동기화 재시도" + "재오픈" 버튼(노션 미설정이라 `notion_synced_at`은 계속 null — 정상).
4. 재오픈 → confirm → "진행중", 확인 배지 4개 유지, 마감 버튼 즉시 활성(part_confirmations 유지).
5. 단원 행 탭 → 출석으로 수정 → 재마감 → 정상.
6. 홈으로 이동 후 네트워크 탭에서 `/board` 폴링이 멈췄는지 확인(cleanup).
7. 모바일 뷰포트에서 가로 스크롤 없음, 바텀 시트가 화면 하단에 붙음.

- [ ] **Step 6: 전체 테스트 + 커밋**

Run: `pytest --basetemp=.pytest_tmp -q`
Expected: 전부 PASS.

```bash
git add app/static/app.js app/static/style.css
git commit -m "feat: 현황판 - 5초 폴링·확인 필요 우선 정렬·수정 시트·마감·재오픈"
```

---

### Task 6: README 갱신 + 최종 점검

**Files:**
- Modify: `README.md`

- [ ] **Step 1: README "실행" 절 아래에 추가**

```markdown
## 화면

`python -m app.main` 후 `http://localhost:8000/` 접속. 프런트는 `app/static/`의
순수 HTML+JS(빌드 없음)이며 같은 서버가 서빙한다. 설계:
docs/superpowers/specs/2026-08-23-frontend-design.md

로컬 확인용 명단 시드(노션 없이):

    python -c "from tests.conftest import SEED; from app import db; c=db.connect('attendance.db'); c.executemany('INSERT OR IGNORE INTO members (name, student_id, part, role) VALUES (?,?,?,?)', SEED); c.commit()"
```

- [ ] **Step 2: 스펙 §9 시나리오 전체 재확인**

Task 3·4·5의 브라우저 확인을 한 번에 순서대로 수행: 단원 지각 입력 → 파트장 결석 처리·확인 → 지휘자 연습 생성·수정·4파트 대행·마감·재오픈·재마감 → 단원 `#/board` 403 → 375px 가로 스크롤 없음. 실패하는 항목이 있으면 해당 태스크로 돌아가 고친 뒤 다시 확인.

- [ ] **Step 3: 최종 테스트 + 커밋**

Run: `pytest --basetemp=.pytest_tmp -q`
Expected: 전부 PASS.

```bash
git add README.md
git commit -m "docs: README에 프런트 접속·시드 안내"
```
